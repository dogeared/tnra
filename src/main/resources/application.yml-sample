spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://localhost:3306/<DB>
    username: <username>
    password: <password>
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
