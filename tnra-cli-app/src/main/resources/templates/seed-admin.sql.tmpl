-- Seed admin user for TNRA group: {{GROUP_NAME}}
-- Run AFTER the app has started (Flyway creates the schema on first start)
-- Generated by tnra-cli on {{DATE}}

INSERT INTO `{{DB_NAME}}`.users (email, first_name, last_name, active)
VALUES ('{{ADMIN_EMAIL}}', '{{ADMIN_FIRST_NAME}}', '{{ADMIN_LAST_NAME}}', 1);
