# Generated by tnra-cli for group: {{GROUP_NAME}}
# Date: {{DATE}}
#
# Host-accessible URLs for IDE development. When running as a Docker
# container, docker-compose.yml overrides SPRING_DATASOURCE_URL to
# the Docker-internal MySQL hostname.

# --- Database ---
SPRING_DATASOURCE_USERNAME={{DB_USER}}
SPRING_DATASOURCE_PASSWORD={{DB_PASSWORD}}
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3307/{{DB_NAME}}
SPRING_FLYWAY_BASELINE_ON_MIGRATE=true
SPRING_FLYWAY_BASELINE_VERSION=1
SPRING_JPA_HIBERNATE_DDL-AUTO=validate

# --- Keycloak ---
KEYCLOAK_CLIENT_ID={{GROUP_NAME}}-app
KEYCLOAK_CLIENT_SECRET={{KEYCLOAK_CLIENT_SECRET}}
KEYCLOAK_ISSUER_URI=https://auth.{{DOMAIN}}/realms/{{REALM_NAME}}

# --- App ---
VAADIN_PRODUCTIONMODE=true
TNRA_NOTIFY_SCHEDULE=0 0 12 * * ?

# --- Email (optional) ---
MAILGUN_KEY_PRIVATE=
MAILGUN_KEY_PUBLIC=
MAILGUN_URL=
