spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://localhost:3306/<DB>
    username: <username>
    password: <password>
  flyway:
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration
